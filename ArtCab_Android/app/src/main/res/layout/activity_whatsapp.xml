<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    tools:context=".WhatsappActivity">

    <LinearLayout
        android:id="@+id/whatsapp_container"
        android:layout_centerInParent="true"
        android:padding="16dp"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:text="Your WhatsApp Number?"
            android:textSize="34sp"
            android:padding="8dp"
            android:textColor="@color/fontColorWhite"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <EditText
            android:id="@+id/whatsapp"
            android:background="@color/fontColorWhite"
            android:textSize="24sp"
            android:inputType="phone"
            android:padding="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </LinearLayout>

    <Button
        android:id="@+id/goto_quote"
        android:text="@string/next"
        android:layout_marginTop="32dp"
        android:textColor="@color/fontColorWhite"
        android:background="@color/colorPrimaryDark"
        android:layout_below="@+id/whatsapp_container"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/skip_whatsapp"
        android:layout_marginTop="32dp"
        android:background="@color/colorPrimaryDark"
        android:layout_below="@+id/goto_quote"
        android:text="@string/skip"
        android:layout_centerInParent="true"
        android:textColor="@color/fontColorWhite"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

</RelativeLayout>